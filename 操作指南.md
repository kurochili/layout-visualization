# 机位图可视化系统 - 操作指南

## 问题：无法打开应用

### 原因分析
系统检测到您的电脑可能**没有安装 Node.js**，这是运行本项目所必需的。

---

## 解决方案

### 方案一：安装 Node.js（推荐）

#### 步骤 1：下载 Node.js
1. 访问 Node.js 官网：https://nodejs.org/
2. 点击下载 **LTS 版本**（长期支持版本，推荐）
3. 下载 Windows Installer (.msi) 文件

#### 步骤 2：安装 Node.js
1. 双击下载的 `.msi` 文件
2. 按照安装向导操作：
   - 点击"下一步"
   - 接受许可协议
   - 选择安装路径（默认即可）
   - **重要**：确保勾选 "Add to PATH" 选项
   - 点击"安装"
3. 安装完成后，**重启电脑**（或重启终端）

#### 步骤 3：验证安装
打开 PowerShell 或命令提示符，输入：
```bash
node --version
npm --version
```
如果显示版本号（如 v18.x.x），说明安装成功！

#### 步骤 4：安装项目依赖
在项目目录 `d:\cursor\1` 中打开终端，运行：
```bash
npm install
```
等待安装完成（可能需要几分钟）

#### 步骤 5：启动应用
运行：
```bash
npm run dev
```

#### 步骤 6：打开浏览器
终端会显示：
```
  ➜  Local:   http://localhost:3000/
```
在浏览器中访问 `http://localhost:3000` 即可使用！

---

### 方案二：使用在线工具（临时方案）

如果您暂时无法安装 Node.js，可以使用在线 KML 查看器：
- Google Earth Web：https://earth.google.com/web/
- 上传您的 KML/KMZ 文件查看

---

## 常见问题

### Q1: 提示 "npm 不是内部或外部命令"
**解决**：Node.js 未正确安装或未添加到 PATH
- 重新安装 Node.js，确保勾选 "Add to PATH"
- 重启电脑后重试

### Q2: npm install 很慢或失败
**解决**：
- 使用国内镜像源：
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### Q3: 浏览器显示 "无法访问此网站"
**解决**：
- 确保 `npm run dev` 命令正在运行
- 检查终端中显示的端口号（默认是 3000）
- 尝试访问 `http://127.0.0.1:3000`

### Q4: 地图不显示
**解决**：
- 检查网络连接（需要加载地图瓦片）
- 如果在中国大陆，可能需要配置代理或使用国内地图服务

---

## 快速检查清单

- [ ] 已安装 Node.js（运行 `node --version` 有输出）
- [ ] 已安装 npm（运行 `npm --version` 有输出）
- [ ] 已运行 `npm install`（存在 node_modules 文件夹）
- [ ] 已运行 `npm run dev`（终端显示服务器地址）
- [ ] 浏览器已打开 `http://localhost:3000`

---

## 需要帮助？

如果按照以上步骤操作后仍有问题，请提供：
1. 错误信息的截图或文字
2. 您执行到哪一步
3. 终端中显示的具体错误






