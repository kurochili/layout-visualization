# 机位图可视化系统 - 使用说明

## 🚀 快速开始（3步搞定）

### 第一步：打开程序
1. 找到文件：`机位图可视化.html`
2. **双击**这个文件
3. 会自动在浏览器中打开（推荐使用 Chrome、Edge 或 Firefox）

### 第二步：上传文件
1. 点击页面顶部的 **"上传KML/KMZ文件"** 按钮
2. 选择你的 KML 或 KMZ 文件
3. 等待几秒钟，系统会自动解析文件

### 第三步：查看地图
- 地图会自动显示所有机位点
- 点击机位点可以查看详细信息
- 查看图例了解不同颜色的含义

---

## 📋 详细功能说明

### 1. 机位点颜色说明

| 颜色 | 含义 | 说明 |
|------|------|------|
| 🟢 **绿色** | 现有机位点 | 当前正在使用的机位点 |
| 🔵 **蓝色** | 未来机位点 | 计划新增的机位点（名称包含"未来"、"计划"、"新增"等） |
| 🟡 **黄色** | 修改的机位点 | 需要修改的机位点（名称包含"修改"、"更新"等） |

### 2. 界面元素说明

- **左上角图例**：显示各种机位点和风场的图例说明
- **主地图**：显示所有机位点的位置和地形
- **右下角小地图**：显示风场位置，不遮挡主地图
- **底部信息栏**：显示机位点统计信息

### 3. 交互操作

- **点击机位点**：弹出窗口显示详细信息（名称、状态、坐标、海拔等）
- **鼠标滚轮**：缩放地图
- **拖拽地图**：移动地图视角
- **双击地图**：快速放大

---

## 📁 支持的文件格式

### KML 文件
- 标准格式：`.kml`
- 包含机位点的坐标和描述信息

### KMZ 文件
- 压缩格式：`.kmz`（实际上是 ZIP 压缩的 KML）
- 支持包含多个文件的 KMZ 文件

---

## 💡 使用技巧

### 技巧1：如何让机位点自动分类？

在 KML 文件的机位点名称或描述中添加关键词：

**未来机位点**（会显示为蓝色）：
- 名称包含：`未来`、`计划`、`新增`
- 例如：`未来机位点1`、`计划新增机位`、`新增机位A`

**修改的机位点**（会显示为黄色）：
- 名称包含：`修改`、`更新`
- 例如：`修改机位点2`、`更新机位B`

**现有机位点**（会显示为绿色）：
- 其他所有机位点默认显示为绿色

### 技巧2：查看详细信息

点击任意机位点，会显示：
- 机位点名称
- 状态（现有/未来/修改）
- 详细描述（如果有）
- 精确坐标（经纬度）
- 海拔高度（如果有）

### 技巧3：地图操作

- **放大缩小**：使用鼠标滚轮或地图右下角的 `+` `-` 按钮
- **移动地图**：按住鼠标左键拖拽
- **重置视野**：双击地图任意位置

---

## ❓ 常见问题

### Q1: 双击HTML文件没有反应？
**解决方法**：
- 右键点击文件 → 选择"打开方式" → 选择浏览器（Chrome、Edge、Firefox等）
- 或者直接拖拽HTML文件到浏览器窗口

### Q2: 地图显示空白？
**可能原因**：
- 网络连接问题（需要加载在线地图）
- 浏览器阻止了某些内容

**解决方法**：
- 检查网络连接
- 尝试刷新页面（按 F5）
- 更换浏览器试试

### Q3: 上传文件后没有显示机位点？
**可能原因**：
- KML文件格式不正确
- 文件中没有有效的坐标数据

**解决方法**：
- 检查KML文件是否包含 `<Placemark>` 和 `<coordinates>` 标签
- 确认坐标格式正确（经度,纬度,海拔）

### Q4: 机位点颜色不对？
**解决方法**：
- 检查机位点名称或描述中是否包含关键词
- 如果没有关键词，默认显示为绿色（现有机位点）
- 可以修改KML文件中的名称来改变颜色

### Q5: 小地图不显示？
**解决方法**：
- 小地图会在上传文件并解析成功后自动显示
- 如果没显示，尝试刷新页面重新上传文件

---

## 🎯 使用示例

### 示例1：查看现有机位图
1. 准备一个包含现有机位点的KML文件
2. 打开 `机位图可视化.html`
3. 上传KML文件
4. 所有机位点会显示为绿色圆点
5. 点击任意机位点查看详细信息

### 示例2：查看包含未来计划的机位图
1. 在KML文件中，将计划新增的机位点命名为：`未来机位点1`、`计划新增机位A` 等
2. 上传文件后，这些机位点会显示为蓝色
3. 可以清楚区分现有和未来的机位点

### 示例3：查看需要修改的机位图
1. 在KML文件中，将需要修改的机位点命名为：`修改机位点2`、`更新机位B` 等
2. 上传文件后，这些机位点会显示为黄色
3. 方便识别需要修改的机位点

---

## 📞 需要帮助？

如果遇到问题：
1. 检查浏览器控制台是否有错误信息（按 F12 打开开发者工具）
2. 确认KML/KMZ文件格式正确
3. 确认网络连接正常（需要加载在线地图）

---

## ✨ 功能特点

✅ **无需安装** - 双击即可使用  
✅ **支持地形** - 显示地形图层  
✅ **自动分类** - 根据名称自动分类机位点  
✅ **图例说明** - 清晰的图例说明  
✅ **小地图** - 右下角显示风场位置  
✅ **详细信息** - 点击查看机位点详细信息  
✅ **统计信息** - 底部显示机位点统计  

---

**祝您使用愉快！** 🎉






